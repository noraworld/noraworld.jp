$(function() {
  $('#typed').typed({
    strings: ['Great discussions are happening here.'],
    typeSpeed: 40,
    startDelay: 1500,
    loop: false,
    cursorChar: '',
    contentType: 'html',
  });
});
